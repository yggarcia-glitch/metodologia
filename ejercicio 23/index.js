"use strcit"
//declaracione de funcion
function cuadrado (a) {
    return a*a;
}
alert (cuadrado (2))

//funciones de expresion

let cuadrado2 = function (a) {
    return a*a;
}
alert (cuadrado2 (3))


//funciones flecha

let cuadrado3 = (a) => a*a;
alert (cuadrado3 (4))

