"use strcit"

let numeroingresado = Number (prompt("ingresar un numero")); 
function cuadradodeunnumero (numero) {

let resultado
resultado = numero * numero

alert ("el resultado del cuadrado de " + numero + " es: " + resultado)
}
cuadradodeunnumero (numeroingresado)