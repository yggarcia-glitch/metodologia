"use strict"

let a = prompt("valor a");
let b = prompt("valor b");

alert ("valor a > b " + (a>b));
alert ("valor a >= b " + (a>=b));
alert ("valor a < b " + (a<b));
alert ("valor a <= b " + (a<=b));
alert ("valor a == b " + (a==b)); //igualdad
alert ("valor a != b " + (a!=b)); // diferente

//ingresar un numero y verifica si es positivo

